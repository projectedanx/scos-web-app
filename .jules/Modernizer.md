## Modernizer — Loose Logical OR Fallbacks Evolution
**Learning:** Legacy loose logical OR (`||`) fallbacks are pervasive in parsing code that maps deep object configurations or properties, introducing risks of false-falsy overrides (e.g. valid empty strings or `0` values being discarded). Replacing them with the strict nullish coalescing operator (`??`) ensures that only explicit `null` or `undefined` triggers the default fallback. A single-file constraint is strictly enforced to prevent cascading scope failures across the broader codebase. Additionally, utilizing native environment execution flags (`--experimental-strip-types`) bypasses missing dependencies and honors the directive to adapt to the existing stack without bootstrapping foreign packages.
**Action:** When updating syntax in isolated modules, strictly verify the AST modification preserves all closure scopes and logical intents (e.g., leaving boolean flow control checks like `if (!length || !otherLength)` intact) while explicitly evading dependency modifications in `package.json` or lockfiles when validating changes with native test tools.

## Modernizer — Nullish Coalescing Evolution in UI Views
**Learning:** Multiple UI view components (such as `CapsuleLabView`, `PromptForgeView`, `PromptLibraryView`, and `ContractBuilderView`) relied heavily on loose logical OR (`||`) for handling default variables, providing default state initialization objects, and rendering component defaults (e.g. `prompt.description || "No description provided."`). This created vulnerability against false-falsy state evaluations. By migrating these to strict nullish coalescing (`??`), default UI properties correctly preserve empty strings or zeros when explicitly defined, ensuring better runtime structural accuracy without risking UI lifecycle regressions.
**Action:** Consistently replace `||` with `??` when defining component defaults or configuring template variables. Ensure that actual boolean conditional disjunctions (e.g., `if (!editingPrompt.title || !editingPrompt.content)`) remain as `||` to prevent breaking operational flow control logic.

## Modernizer — Nullish Coalescing Evolution in Main App Component
**Learning:** The root `App.tsx` component utilized loose logical OR (`||`) when parsing strings from `localStorage.getItem()`. Since `localStorage.getItem()` returns either a `string` or `null`, a strict nullish coalescing operator (`??`) accurately captures the intended missing state (null) without the risk of false-falsy masking on an empty string `""` (which would throw a parsing error). This enhances runtime strictness for offline fallback states.
**Action:** Continue strictly updating legacy fallback syntax to modern `??` where types map clearly to `null` or `undefined`, avoiding changes to genuine boolean flow control expressions. Ensure changes are verified locally utilizing native stack compilation (`npx tsc --noEmit`).

## Modernizer — Pruning Logical OR Fallbacks in Service Configuration
**Learning:** In `services/conductorService.ts`, legacy logical OR (`||`) operators were used extensively for fallback values when constructing Swarm configurations (`agent.protocol?.standard || 'DRP-2025'`) and generating template literals. Transforming these instances to strictly use nullish coalescing (`??`) clarifies the developer's intent and structurally guards against potential false-falsy edge cases (such as an empty string being explicitly passed as a configuration value).
**Action:** Consistently replace `||` with `??` across service files when handling default assignments, taking extra care to isolate changes to a single target file to respect blast-radius restrictions. Furthermore, ensure coverage and test artifacts generated natively (e.g., via `npx c8`) are explicitly cleaned from the repository prior to code reviews.

## Modernizer — Nullish Coalescing Evolution in Word Mapper View
**Learning:** In `views/WordMapperView.tsx`, deep object property access fallback patterns like `def || node.definition || "No further definition available."` were identified. This specific usage risks discarding perfectly valid falsy values. Applying strict nullish coalescing cleanly upgrades the AST and explicitly prevents false-falsy regressions within Wikipedia definitions without altering application flow control loops.
**Action:** Always document syntax modernization mutations in the Modernizer journal before PR submission, strictly conforming to the Prune-First protocol to prevent LLM context collapse.

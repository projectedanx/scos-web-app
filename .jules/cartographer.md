Learning: VANCE requires a strict bidirectional DAG model and incremental syntax parsing to prevent standard generative AI hallucination cascades and causal asymmetry in semantic indexing tasks (e.g., `textDocument/references`).
Action: The VANCE agent profile has been saved to `prompts/VANCE-v1.0-SOVEREIGN.md` and registered in `STATE.md` and `UPDATES.md`.
